Hi,

Good news! A new ride offer was posted that matches your request for the event "<%= @ride_request.event.name %>".

You can view it here:

    <%= event_entry_url(@entry.event, @entry, locale: I18n.locale) %>

There may be more offers as well — see all offers for this event here:

    <%= event_url(@ride_request.event, locale: I18n.locale) %>

If you no longer want to receive notifications for this request, you can delete it with this secret link:

    <%= event_ride_request_destroy_url(@ride_request.event, @ride_request, token: @ride_request.token, locale: I18n.locale) %>


Kind regards from the HopHub platform.
